name: heyflutter_codding_challenge
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2


  # utility
  readmore: ^2.2.0
  flutter_rating_bar: ^4.0.1

  # image
  photo_view: ^0.14.0

  # map
  latlong2: ^0.9.0
  flutter_map:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


  assets:
     - assets/images/

